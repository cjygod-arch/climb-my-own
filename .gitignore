# Overpass 응답 캐시. 재생성 가능하고 용량이 크다.
# 지우면 tools/snap-waypoints.mjs · fetch-tracks.mjs 가 다시 내려받는다.
.cache/

# 편집기 · OS · 도구 로컬 설정
.DS_Store
Thumbs.db
.vscode/
.idea/
.claude/

# 임시 산출물
*.log
shots*/

# 개인 메모. 공개 저장소이므로 add -A 에 딸려 올라가지 않도록 막는다.
# 배포용으로 정리한 판은 docs/설치안내.txt 에 있다.
참고매뉴얼.txt
